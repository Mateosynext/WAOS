/**
 * @deprecated Keep this file as a type-only compatibility surface. Import screen
 * components directly from reconfigure/screens/* so each reconfigure step can be lazy loaded.
 */
export type {
  ReconfigureDiffViewModel,
  ReconfigureSelectActions,
  ReconfigureSelectViewModel,
  ReconfigureValidationViewModel,
  ReconfigureWizardActions,
  ReconfigureWizardViewModel,
} from "./reconfigureScreenTypes";
